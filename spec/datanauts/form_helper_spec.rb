require 'spec_helper'

describe Datanauts::FormHelper do
  it 'has a version number' do
    expect(Datanauts::FormHelper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
